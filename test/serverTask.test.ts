import ServerTask from '../src/task/serverTask';

describe('Server Task Class', () => {
  it('should exist', () => {
    expect(ServerTask).to.exist;
  });

  describe('RPC task', () => {
    it('should ');
  });
});
